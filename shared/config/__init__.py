# shared/config
#
# Centralised configuration helpers: environment variable loading, service
# URLs, and any constants shared across data-etl, models, and backtesting.
#
# Example usage:
#   from shared.config import get_spark_session, MINIO_ENDPOINT
